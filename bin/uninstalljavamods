#!/bin/bash

. "$(dirname "$(dirname "${0}")")/scripts/pzenv"

ARCHIVE='javamods-linux.zip'

if ! [ -d "${PZSERVER}" ]; then
	echo No installed pzserver found
	exit
fi

rm -f \
	"${PZSERVER}/${ARCHIVE}"               \
	"${PZSERVER}/javamods.json"            \
	"${PZSERVER}/javamods-server-start.sh" \
	"${PZSERVER}/java/javamods.jar"

echo OK
